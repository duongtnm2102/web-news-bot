# Tiền Phong E-con News Portal - ULTRA-FAST VERSION
# Environment Variables for 30s Timeout Compatibility

# === REQUIRED VARIABLES ===

# Gemini AI API Key for content analysis
# Get from: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# Secret key for Flask sessions (generate a random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here

# === OPTIONAL VARIABLES (Auto-set by Render) ===

# Port (Render will set this automatically)
PORT=8080

# Python version
PYTHON_VERSION=3.11.0

# === PRODUCTION SETTINGS ===

# Flask environment
FLASK_ENV=production
FLASK_DEBUG=False

# Python settings for better performance
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# Web concurrency (keep low for free tier)
WEB_CONCURRENCY=1

# === ULTRA-FAST CONFIGURATION ===

# Maximum processing time (seconds) - keep under 25s
MAX_PROCESSING_TIME=20

# Maximum concurrent sources to fetch
MAX_SOURCES_CONCURRENT=2

# Maximum articles per RSS source
MAX_ARTICLES_PER_SOURCE=3

# Request timeout per RSS feed (seconds)
REQUEST_TIMEOUT=3

# === DEPLOYMENT NOTES ===

# 1. Copy this file to .env
# 2. Replace "your_*_here" with actual values
# 3. Add environment variables in Render dashboard:
#    - GEMINI_API_KEY
#    - SECRET_KEY
# 4. Use this start command in Render:
#    gunicorn app:app --bind 0.0.0.0:$PORT --timeout 25 --workers 1 --threads 2
# 5. Set health check path: /health
