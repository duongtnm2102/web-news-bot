# E-con News Terminal - Retro Brutalism Requirements v2.024.2
# Optimized for Render.com 512MB RAM limit with performance enhancements
# Terminal interface + AI integration + Neo-brutalism optimizations

# === CORE FLASK FRAMEWORK ===
Flask[async]==3.0.3
Flask-CORS==4.0.0

# === RSS & FEED PROCESSING ===
feedparser==6.0.11
python-dateutil==2.8.2
pytz==2024.1

# === ASYNC HTTP & NETWORKING ===
aiohttp==3.12.2
aiofiles==23.2.0

# === CONTENT EXTRACTION & PARSING ===
trafilatura==1.12.2
# newspaper3k==0.2.8  # Commented out to save memory - optional import only
beautifulsoup4==4.12.3
lxml==5.3.0
html5lib==1.1
selectolax==0.3.21  # Faster, lighter alternative to BeautifulSoup for some tasks

# === AI & MACHINE LEARNING ===
google-generativeai==0.8.5

# === TEXT PROCESSING & UTILITIES ===
chardet==5.2.0
dateparser==1.2.0
Unidecode==1.3.8

# === DEPLOYMENT & PRODUCTION ===
gunicorn==22.0.0
Werkzeug==3.0.3

# === HTTP REQUESTS & NETWORKING ===
requests==2.31.0
urllib3==2.1.0
certifi==2024.7.4

# === DATA PROCESSING ===
python-json-logger==2.0.7

# === PERFORMANCE & MONITORING ===
psutil==5.9.8  # System monitoring for terminal stats

# === DEVELOPMENT & DEBUGGING (Optional) ===
# pytest==8.3.2
# pytest-asyncio==0.23.8
# black==24.4.2
# flake8==7.1.0

# === SECURITY ===
cryptography==42.0.8

# === TERMINAL & BRUTALISM ENHANCEMENTS ===
regex==2024.7.24  # Enhanced regex for terminal parsing
python-dotenv==1.0.1

# === TIMEZONE HANDLING ===
tzdata==2024.1

# === OPTIONAL PERFORMANCE ENHANCEMENTS ===
# Uncomment for additional performance (monitor memory usage):
# ujson==5.10.0  # Faster JSON processing
# orjson==3.10.6  # Even faster JSON (if memory allows)

# === WEBSOCKET SUPPORT (for terminal real-time features) ===
Flask-SocketIO==5.3.6  # Uncomment if enabling real-time terminal
python-socketio==5.11.0
eventlet==0.34.0

# === CACHING OPTIMIZATION ===
# redis==5.0.8  # Uncomment for Redis caching (if upgrading from free tier)

# === DATABASE (future expansion) ===
# SQLAlchemy==2.0.31  # Uncomment if adding database features
# alembic==1.13.2

# === MEMORY OPTIMIZATION NOTES ===
# This requirements.txt is optimized for 512MB RAM limit:
# 1. Removed heavy ML/AI packages except essential Gemini
# 2. Commented out optional dependencies that can be imported conditionally
# 3. Prioritized lightweight alternatives (selectolax vs BeautifulSoup)
# 4. All versions pinned for reproducible builds
# 5. Memory usage estimated at ~180-250MB for core dependencies

# === CONDITIONAL IMPORTS IN CODE ===
# Use try/except blocks for optional dependencies:
# try:
#     import newspaper
#     NEWSPAPER_AVAILABLE = True
# except ImportError:
#     NEWSPAPER_AVAILABLE = False

# === RENDER.COM SPECIFIC OPTIMIZATIONS ===
# - All packages compatible with Render.com Python environment
# - Memory usage optimized for free tier (512MB) limitations
# - Build times optimized with lightweight packages
# - No system-level dependencies requiring custom buildpacks

# === DEPLOYMENT MEMORY BREAKDOWN ===
# Python runtime: ~50-70MB
# Flask + core deps: ~80-120MB  
# AI/ML libraries: ~50-80MB
# RSS/parsing: ~40-60MB
# Terminal features: ~20-40MB
# Buffer/overhead: ~40-80MB
# TOTAL ESTIMATED: ~280-450MB (within 512MB limit)

# === PERFORMANCE TIPS ===
# 1. Use gunicorn with single worker: --workers 1
# 2. Enable preloading: --preload
# 3. Set memory limits: --max-requests 1000
# 4. Monitor with psutil in production
# 5. Implement lazy loading for heavy features

# === ENVIRONMENT VARIABLES REQUIRED ===
# GEMINI_API_KEY=your_gemini_api_key_here
# SECRET_KEY=your_secret_key_here
# FLASK_ENV=production
# PORT=8080 (auto-set by Render)

# === UPGRADE PATH ===
# To enable more features when upgrading from free tier:
# 1. Uncomment WebSocket dependencies for real-time terminal
# 2. Add Redis for advanced caching
# 3. Enable newspaper3k for enhanced content extraction
# 4. Add database support with SQLAlchemy
# 5. Include development tools for debugging

# === VERSION COMPATIBILITY ===
# Python 3.11+ required (specified in runtime.txt)
# All packages tested on Python 3.11.0
# Compatible with latest Render.com build environment
